Convert string

    Problem Statement :

        - S1 and S2 are given strings
        - Convert S2 to S1 using delete, insert or replace operations 
        - Find the minimum count of edit operations 

    Example 1 

        - S1 = "catch"
        - S2 = "catch"
        - Output = 1 
        - Explanation : replace "r" with "t"

    Example 2

        - S1 = "table"
        - S2 = "tbres"
        - Explanation : Insert "a" to second position, replace "r" with "I" and delete "s"

    Example 

        - S1 = "table"
        - S2 = "tgable"     Delete  ----------> f (2,3)

        - S1 = "table"
        - S2 = "tble"       Insert  ----------> f (3,2)

        - S1 = "table"
        - S2 = "tcble"      Replace ----------> f (3,3)

    findMinOperations(s1, s2, index1, index2):
        if index1 == len(s1)
            return len(s2)-index2 
        If index2 == len(s2)
            return len(s1)-index1
        If s1[index1] == s2[index2]
            return findMinOperations(s1, index1+1, index2+1)

        Else 
            deleteOp = 1 + findMinOperations(s1, s2, index1, index2+1)
            insertOp = 1 + findMinOperations(s1, s2, index+1, index2)
            replaceOp = 1 + findMinOperation(s1, s2, index1+1, index2+1)
            return min(deleteOp, insertOp, replaceOp)

