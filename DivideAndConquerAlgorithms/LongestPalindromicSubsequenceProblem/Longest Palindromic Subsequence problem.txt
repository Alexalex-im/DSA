The Longest Palindromic Subsequence (LPS)

Problem Statement:

    - S is a given string 
    - Fin the longest palindromic subsequence (LPS)
    - subsequence a sequence that can be driven from another sequence by deleting some elements without changing the order of them 
    - Palindrome is a string that reads the same backward as well as forward.

    
    MADAM

    Example 1 
        - S = "ELRMENMET"
        - Output  = 5
        - LPS : "EMEME"

    Example 2 

        - S = "AMEEWMEA"
        - Output = 6
        - LPS : "AMEEMA"

    Subproblems: 
        Option1 = 2 + f(2,8)
        Option2 = 0 + f(1,8)   ----------> Max(Option1, Option2, Option3)
        Option3 = 0 + f(2,9)

    findLPS(S, startIndex, endIndex) :
        If startIndex > endIndex:
            return 0
        If s1[startIndex] == s2[endIndex-1]
            return 2 + findLPS(s, startIndex+1, endIndex+1)

        Else 
            op1 = findCLS(s, startIndex, endIndex-1)
            op2 = findCLS(s, startIndex+1, endIndex)
            return max(op1, op2)
        